<h1>メッセージ一覧</h1>

<ul>
  <% @messages.each do |message| %>

    <li>
      <%= link_to message_id, message %>
      <%= message.content %>
    </li>

  <% end %>
</ul>

<%= link_to '新規投稿', new_message_path %>